# Unit test utilities

This folder is reserved for shared helpers used by unit tests.

Use this folder for reusable test-only code, such as:

- test data builders;
- object mothers;
- fake clocks;
- fake current user providers;
- custom assertions;
- reusable test constants.

Keep this folder focused on helpers that are actually used by real tests.

Do not add generic utilities before they are needed.
